---
- name: Reload sysctl
  ansible.builtin.command: sysctl --system
  changed_when: false

- name: Reload systemd and enable nat service
  ansible.builtin.systemd:
    daemon_reload: true
    name: nat-masquerade.service
    enabled: true
    state: started